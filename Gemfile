source "https://rubygems.org"

# Manage Jekyll version via GitHub Pages
gem "github-pages", group: :jekyll_plugins

# Optional plugins
group :jekyll_plugins do
  gem "jekyll-feed"
  gem "jekyll-sitemap"
  gem "hawkins"
  gem "webrick", "~> 1.8"
end

gem "tzinfo-data", platforms: [:mingw, :mswin, :x64_mingw, :jruby]
